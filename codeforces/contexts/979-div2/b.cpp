#include<bits/stdc++.h>
//#define int long long
using namespace std;



signed main() {
	cin.tie(0);
	cout.tie(0);
	ios_base::sync_with_stdio(0);
	int nTests; cin>>nTests;
	for (int t = 0; t < nTests; t++) {
		int n; cin>>n;
		int val = 0;
		for (int i = 0; i < n-1; i++) {
			cout<<val;
		}
		cout<<1<<endl;
	}

	return 0;
}